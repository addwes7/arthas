name: JavaCI

on:
  workflow_dispatch:

jobs:
  ubuntu_build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        java: [8, 11, 17, 21, 25]
        os:
          - ubuntu-latest
          - windows-latest
          - macos-15-intel
      fail-fast: false
    steps:
      - uses: actions/checkout@v4
      - name: Setup java
        uses: actions/setup-java@v4
        with:
          java-version: ${{ matrix.java }}
          distribution: "zulu"
          cache: "maven"
      - name: Build with Maven
        run: mvn -V -ntp clean install -P full verify

  windows_build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        java: [8, 11]
        os:
          - ubuntu-latest
          - windows-latest
          - macos-15-intel
      fail-fast: false
    steps:
      - uses: actions/checkout@v4
      - name: Setup java
        uses: actions/setup-java@v4
        with:
          java-version: ${{ matrix.java }}
          distribution: "zulu"
          cache: "maven"
      - name: Build with Maven
        run: mvn -V -ntp clean install -P full

  macos_build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        java: [8, 11]
        os:
          - macos-15-intel
          - macos-14
          - ubuntu-latest
          - windows-latest
      fail-fast: false
    steps:
      - uses: actions/checkout@v4
      - name: Setup java
        uses: actions/setup-java@v4
        with:
          java-version: ${{ matrix.java }}
          distribution: "zulu"
          cache: "maven"
      - name: Build with Maven
        run: mvn -V -ntp clean install -P full

  telnet_stop_leak_it:
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Setup java
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: "zulu"
          cache: "maven"
      - name: Install expect/telnet
        run: sudo apt-get update && sudo apt-get install -y expect telnet
      - name: Build packaging
        run: mvn -V -ntp clean install -P full -DskipTests
      - name: Run telnet stop leak test
        run: python3 integration-test/telnet-stop-leak/run_telnet_stop_leak_test.py --iterations 10 --warmup 2 
          --threshold 5 --work-dir integration-test/telnet-stop-leak/work
      - name: Upload artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: telnet-stop-leak-logs
          path: integration-test/telnet-stop-leak/work
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
          - macos-latest
      fail-fast: false
